<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>com.nexora</groupId>
        <artifactId>nexora-server</artifactId>
        <version>1.0-SNAPSHOT</version>
    </parent>

    <artifactId>nexora-common</artifactId>
    <description>通用模块</description>

    <dependencies>
        <!-- Spring Boot（BOM 已统一排除 spring-boot-starter-logging，下游无需再写 exclusion） -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <!-- Redis JSON serialization for Java 8+ date/time values -->
        <dependency>
            <groupId>com.fasterxml.jackson.datatype</groupId>
            <artifactId>jackson-datatype-jsr310</artifactId>
        </dependency>

        <!-- Bean Validation -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>

        <!-- AOP -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-aop</artifactId>
        </dependency>

        <!-- 平台 webmvc 通用件：传递引入 Log4j2 与默认 log4j2-spring.xml，
             同时复用 Result / BizException / GlobalExceptionHandler / TraceIdFilter 等基础设施 -->
        <dependency>
            <groupId>io.github.wuhobin</groupId>
            <artifactId>platform-webmvc</artifactId>
        </dependency>

        <!-- MyBatis Plus（platform-boot-starter；BOM 已统一排除默认 logging） -->
        <dependency>
            <groupId>io.github.wuhobin</groupId>
            <artifactId>mybatis-plus-spring-boot-starter</artifactId>
        </dependency>

        <!-- Quartz 定时任务（platform-boot-starter；BOM 已统一排除默认 logging） -->
        <dependency>
            <groupId>io.github.wuhobin</groupId>
            <artifactId>quartz-spring-boot-starter</artifactId>
        </dependency>

        <!-- Sa-Token 认证（platform-boot-starter；BOM 已统一排除默认 logging） -->
        <dependency>
            <groupId>io.github.wuhobin</groupId>
            <artifactId>sa-token-spring-boot-starter</artifactId>
        </dependency>

        <!-- Redis 缓存（platform-boot-starter） -->
        <dependency>
            <groupId>io.github.wuhobin</groupId>
            <artifactId>redis-spring-boot-starter</artifactId>
        </dependency>

        <!-- 验证码（platform-boot-starter） -->
        <dependency>
            <groupId>io.github.wuhobin</groupId>
            <artifactId>verification-spring-boot-starter</artifactId>
        </dependency>

        <!-- OSS 对象存储（platform-boot-starter） -->
        <dependency>
            <groupId>io.github.wuhobin</groupId>
            <artifactId>oss-spring-boot-starter</artifactId>
        </dependency>

        <dependency>
            <groupId>cn.idev.excel</groupId>
            <artifactId>fastexcel</artifactId>
            <exclusions>
                <exclusion>
                    <groupId>ch.qos.logback</groupId>
                    <artifactId>logback-classic</artifactId>
                </exclusion>
                <exclusion>
                    <groupId>com.itextpdf</groupId>
                    <artifactId>pdftest</artifactId>
                </exclusion>
                <exclusion>
                    <groupId>com.itextpdf</groupId>
                    <artifactId>font-asian</artifactId>
                </exclusion>
                <exclusion>
                    <groupId>org.ehcache</groupId>
                    <artifactId>ehcache</artifactId>
                </exclusion>
                <exclusion>
                    <groupId>junit</groupId>
                    <artifactId>junit</artifactId>
                </exclusion>
            </exclusions>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>

    </dependencies>

</project>
